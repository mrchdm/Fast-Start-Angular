import { HightlightBorderDirective } from './hightlight-border.directive';

describe('HightlightBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightBorderDirective();
    expect(directive).toBeTruthy();
  });
});
